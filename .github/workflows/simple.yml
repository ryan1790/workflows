name: Shell Commands
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-18.04
    steps:
      - name: echo a string
        run: eccho "Hello World"
      - name: multiline script
        run: |
          node -v
          npm -v